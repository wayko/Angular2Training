import { bootstrap } from '@angular/platform-browser-dynamic';

import { AppComponent } from './app.compnent';
bootstrap(AppComponent);

import { NavAppComponent } from './app.compnent';
bootstrap(NavAppComponent);